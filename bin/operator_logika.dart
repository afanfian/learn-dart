// Contoh penggunaan operator logika di dart

void main(){
    var a = true;
    var b = false;

    // Operator logika AND
    print(a && b);
    // Operator logika OR
    print(a || b);
    // Operator logika NOT
    print(!b);
}